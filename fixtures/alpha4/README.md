# Alpha.4 offline fixture graph

This directory contains the immutable tarballs used by `scripts/check-pack.mjs` for the exact DeepSeek Harness `dsh-v0.1.2-alpha.4` dependency plane. Official archives are built from commit `4e84901e6471b79ec0338099867ebb4606d12bb5`; third-party archives are registry snapshots with SHA-256 records in `PROVENANCE.json`.

The Providers UI owner is pinned by content hash. Fixture archives are test inputs only and are not shipped in the plugin package.
